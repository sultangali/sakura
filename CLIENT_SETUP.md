# Настройка клиента для работы с облачным сервером

## Проблема

При локальном запуске Vite (`npm run dev`) клиент пытается обращаться к `localhost:5000`, но локальный сервер не работает. Нужно настроить клиент для работы с облачным сервером `34.88.233.59`.

## Решение

### Вариант 1: Использовать готовый bat файл (Windows)

**Для работы с облачным сервером:**
```batch
start-client-cloud.bat
```

Этот файл автоматически:
- Создаст `.env.local` с настройками для облачного сервера
- Запустит клиент на `localhost:5173`
- Настроит Vite proxy для проксирования запросов на `34.88.233.59`

**Для работы с локальным сервером:**
```batch
start-client-local.bat
```

### Вариант 2: Ручная настройка

#### Шаг 1: Создайте `.env.local` файл

В директории `client/` создайте файл `.env.local`:

```env
# Локальная разработка с облачным сервером
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_FORCE_CLOUD=true
```

#### Шаг 2: Запустите клиент

```bash
cd client
npm run dev
```

## Как это работает

### С `VITE_FORCE_CLOUD=true`:

1. **Vite proxy** (`vite.config.js`):
   - Проксирует `/api` → `http://34.88.233.59/api`
   - Проксирует `/uploads` → `http://34.88.233.59/uploads`

2. **Client config** (`config.js`):
   - В dev режиме использует `/api` (через Vite proxy)
   - Vite автоматически проксирует на облачный сервер

3. **Результат**:
   - Клиент работает на `localhost:5173`
   - API запросы идут через Vite proxy на `34.88.233.59`
   - Нет ошибок CORS, так как запросы идут через proxy

### Без `VITE_FORCE_CLOUD` (по умолчанию):

- Клиент обращается напрямую к `http://localhost:5000/api`
- Требует запущенный локальный сервер

## Проверка

После запуска откройте консоль браузера (F12). Должно показать:

```
═══════════════════════════════════════
🔧 Режим: ЛОКАЛЬНЫЙ (Development)
   Сервер: localhost:5000
   API URL: /api
   Server URL: http://34.88.233.59
   Mode: development
   PROD: false
═══════════════════════════════════════
```

И в терминале Vite должно показать:
```
🔄 Vite Proxy Target: http://34.88.233.59
```

## Файлы

- `start-client-cloud.bat` - Запуск клиента с облачным сервером (Windows)
- `start-client-local.bat` - Запуск клиента с локальным сервером (Windows)
- `client/.env.local` - Конфигурация для локальной разработки (создается автоматически)

## Важно

- `.env.local` имеет приоритет над другими `.env` файлами
- При изменении `.env.local` перезапустите Vite
- Для production используйте `.env.production` (настраивается через `deploy.sh`)





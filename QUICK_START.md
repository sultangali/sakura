# Быстрый старт

## Для Windows

### Запуск клиента с облачным сервером (рекомендуется)

Просто запустите:
```batch
start-client-cloud.bat
```

Это автоматически:
- ✅ Создаст `.env.local` с настройками для облачного сервера
- ✅ Запустит клиент на `http://localhost:5173`
- ✅ Настроит Vite proxy для работы с `34.88.233.59`
- ✅ Откроет браузер

### Запуск клиента с локальным сервером

Если у вас запущен локальный сервер:
```batch
start-client-local.bat
```

## Для Linux/macOS

### Запуск с облачным сервером

```bash
cd client

# Создайте .env.local
cat > .env.local << EOF
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_FORCE_CLOUD=true
EOF

# Запустите клиент
npm run dev
```

### Запуск с локальным сервером

```bash
cd client

# Создайте .env.local
cat > .env.local << EOF
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_FORCE_LOCAL=true
EOF

# Запустите клиент
npm run dev
```

## Проверка

После запуска:
1. Откройте `http://localhost:5173`
2. Откройте консоль (F12)
3. Должно показать:
   ```
   🔧 Режим: ЛОКАЛЬНЫЙ (Development)
      API URL: /api
   ```
4. В терминале Vite должно показать:
   ```
   🔄 Vite Proxy Target: http://34.88.233.59
   ```

## Важно

- **С облачным сервером**: Не требуется запускать локальный сервер
- **С локальным сервером**: Требуется запустить сервер (`cd server && npm run dev`)





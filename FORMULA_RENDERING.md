# Рендеринг формул и обработка изображений

## Что изменилось

### 1. Качественный рендеринг формул с Puppeteer

Теперь формулы конвертируются в PNG изображения с помощью:
- **Puppeteer** - для запуска headless браузера
- **MathJax** - для рендеринга математических формул
- **Sharp** - для оптимизации и обрезки изображений

### 2. Улучшенный парсинг вариантов

- Все варианты теперь сохраняются (включая варианты только с изображениями)
- Варианты с изображениями сохраняются как HTML с `<img>` тегами
- Варианты с формулами конвертируются в PNG через Puppeteer
- Логирование количества вариантов для диагностики

### 3. Медленная, но качественная обработка

- Puppeteer запускает реальный браузер для рендеринга формул
- MathJax полностью обрабатывает формулы перед скриншотом
- Sharp оптимизирует и обрезает пустые края
- Все операции выполняются последовательно для качества

## Установка

### Локально

```bash
cd server
npm install
```

Puppeteer автоматически установит Chromium браузер (~170MB).

### На сервере

```bash
cd /var/www/platonus/server
npm install
pm2 restart platonus-server
```

## Как это работает

### Процесс конвертации формулы:

1. **Обнаружение формулы** - система определяет сложные математические формулы в тексте
2. **Подготовка LaTeX** - формула конвертируется в LaTeX формат
3. **Рендеринг в браузере** - Puppeteer запускает браузер с MathJax
4. **Скриншот** - делается скриншот отрендеренной формулы
5. **Оптимизация** - Sharp обрезает края и оптимизирует размер
6. **Сохранение** - PNG файл сохраняется в `/uploads/`

### Время обработки:

- **Обычные изображения**: ~100-500ms
- **Формулы (Puppeteer)**: ~2-5 секунд на формулу
- **WMF/EMF**: ~100ms (создается placeholder)

## Логирование

При загрузке файла вы увидите:

```
📐 Вариант 2 содержит формулу, конвертируем в PNG...
  ✓ Формула конвертирована для варианта 2
✓ Формула конвертирована в PNG: formula-1234567890-abc123.png
```

## Проблемы и решения

### Puppeteer не запускается

**Ошибка**: `Failed to launch the browser process`

**Решение**:
```bash
# Установите зависимости для Chromium
sudo apt-get update
sudo apt-get install -y \
  ca-certificates \
  fonts-liberation \
  libappindicator3-1 \
  libasound2 \
  libatk-bridge2.0-0 \
  libatk1.0-0 \
  libc6 \
  libcairo2 \
  libcups2 \
  libdbus-1-3 \
  libexpat1 \
  libfontconfig1 \
  libgbm1 \
  libgcc1 \
  libglib2.0-0 \
  libgtk-3-0 \
  libnspr4 \
  libnss3 \
  libpango-1.0-0 \
  libpangocairo-1.0-0 \
  libstdc++6 \
  libx11-6 \
  libx11-xcb1 \
  libxcb1 \
  libxcomposite1 \
  libxcursor1 \
  libxdamage1 \
  libxext6 \
  libxfixes3 \
  libxi6 \
  libxrandr2 \
  libxrender1 \
  libxss1 \
  libxtst6 \
  lsb-release \
  wget \
  xdg-utils
```

### Формулы не конвертируются

**Проверьте**:
1. Puppeteer установлен: `npm list puppeteer`
2. Браузер запускается: проверьте логи сервера
3. MathJax доступен: проверьте интернет-соединение

### Медленная обработка

Это нормально! Качество важнее скорости:
- Формулы обрабатываются последовательно
- Каждая формула требует ~2-5 секунд
- Для 200 вопросов с формулами это может занять 10-20 минут

## Производительность

| Тип контента | Время обработки |
|--------------|----------------|
| Обычный текст | < 1ms |
| PNG/JPG изображение | 100-500ms |
| Формула (Puppeteer) | 2-5 секунд |
| WMF/EMF (placeholder) | 100ms |

## Отключение Puppeteer

Если нужно отключить Puppeteer (для экономии ресурсов):

```bash
npm uninstall puppeteer
```

Формулы будут сохраняться как текст.




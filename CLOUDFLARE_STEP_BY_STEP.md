# Пошаговая настройка Cloudflare SSL

## 🔹 ШАГ 1. Подключение домена к Cloudflare

### ✅ 1.1. Регистрация на Cloudflare
- Зайдите на [cloudflare.com](https://www.cloudflare.com)
- Зарегистрируйтесь или войдите

### ✅ 1.2. Добавление домена
- Нажмите **Add a Site**
- Введите `oxxooy.online`
- Выберите план **Free**
- Cloudflare просканирует DNS — **подтвердите**

### ✅ 1.3. Замена NS-серверов в Namecheap
Cloudflare даст вам два NS-сервера (например):
- `alice.ns.cloudflare.com`
- `bob.ns.cloudflare.com`

**В Namecheap:**
1. Domain List → **oxxooy.online** → **Manage**
2. **Nameservers** → **Custom DNS**
3. Введите NS-серверы от Cloudflare
4. Сохраните
5. **Подождите 5-15 минут** (иногда до часа)

---

## 🔹 ШАГ 2. DNS-настройки (очень важно!)

### ✅ 2.1. Настройка A записей в Cloudflare

1. В Cloudflare: **DNS** → **Records**
2. Удалите старые записи (если есть)
3. Добавьте две A записи:

   **Запись 1:**
   - **Type**: `A`
   - **Name**: `oxxooy.online` (или `@`)
   - **Content**: `34.88.233.59`
   - **Proxy**: 🟠 **ON** (оранжевое облако) ✅
   - **TTL**: `Auto`
   - **Save**

   **Запись 2:**
   - **Type**: `A`
   - **Name**: `www`
   - **Content**: `34.88.233.59`
   - **Proxy**: 🟠 **ON** (оранжевое облако) ✅
   - **TTL**: `Auto`
   - **Save**

**⚠️ КЛЮЧЕВОЙ МОМЕНТ**: 🟠 Оранжевая тучка (Proxy) должна быть **ВКЛЮЧЕНА**!

Именно она:
- ✅ Скрывает IP вашего VPS
- ✅ Даёт CDN ускорение
- ✅ Помогает обойти блокировки университета

---

## 🔹 ШАГ 3. SSL/TLS в Cloudflare

### ✅ 3.1. Получение Origin Certificate

1. В Cloudflare: **SSL/TLS** → **Origin Server**
2. Нажмите **Create Certificate**
3. Настройки:
   - **Private key type**: `RSA (2048)`
   - **Hostnames**: 
     - `oxxooy.online`
     - `*.oxxooy.online`
   - **Validity**: `15 years`
4. Нажмите **Create**
5. **Скопируйте оба блока:**
   - **Origin Certificate**
   - **Private Key**

### ✅ 3.2. Сохранение сертификата на сервере

На сервере выполните:

```bash
# Создаем директорию
sudo mkdir -p /etc/nginx/ssl/oxxooy.online

# Сохраняем сертификат
sudo nano /etc/nginx/ssl/oxxooy.online/cert.pem
# Вставьте Origin Certificate, сохраните: Ctrl+O, Enter, Ctrl+X

# Сохраняем приватный ключ
sudo nano /etc/nginx/ssl/oxxooy.online/key.pem
# Вставьте Private Key, сохраните: Ctrl+O, Enter, Ctrl+X

# Устанавливаем права
sudo chmod 600 /etc/nginx/ssl/oxxooy.online/key.pem
sudo chmod 644 /etc/nginx/ssl/oxxooy.online/cert.pem
```

### ✅ 3.3. Установка режима Full (strict)

1. В Cloudflare: **SSL/TLS** → **Overview**
2. Выберите режим: **Full (strict)** ✅

**Почему Full (strict):**
- ✅ У вас уже есть Origin Certificate
- ✅ Самый безопасный режим
- ✅ Университетские сети ему доверяют

---

## 🔹 ШАГ 4. Проверка nginx (сервер)

### ✅ 4.1. Автоматическая настройка Nginx

```bash
cd /home/sultangali/Documents/platonus-buketov
sudo ./setup-cloudflare-ssl.sh oxxooy.online
```

Скрипт автоматически:
- ✅ Создаст конфигурацию с HTTPS
- ✅ Настроит редирект HTTP → HTTPS
- ✅ Проверит конфигурацию

### ✅ 4.2. Проверка конфигурации

```bash
# Проверка синтаксиса
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx

# Проверка статуса
sudo systemctl status nginx
```

### ✅ 4.3. Проверка портов

```bash
# Проверка, что Nginx слушает на портах 80 и 443
sudo netstat -tuln | grep -E ':(80|443) '
```

Должно быть:
```
tcp  0  0  0.0.0.0:80    LISTEN
tcp  0  0  0.0.0.0:443   LISTEN
```

### ✅ 4.4. Типичная конфигурация

После настройки конфигурация должна содержать:

```nginx
# HTTP - редирект на HTTPS
server {
    listen 80;
    server_name oxxooy.online www.oxxooy.online;
    return 301 https://$host$request_uri;
}

# HTTPS - основной сервер
server {
    listen 443 ssl http2;
    server_name oxxooy.online www.oxxooy.online;

    # Cloudflare Origin Certificate
    ssl_certificate /etc/nginx/ssl/oxxooy.online/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/oxxooy.online/key.pem;

    # ... остальная конфигурация ...
}
```

**⚠️ Важно**: Используйте `cert.pem` и `key.pem` от Cloudflare, а не `fullchain.pem` от Let's Encrypt!

---

## 🔹 ШАГ 5. Включить авто-HTTPS в Cloudflare

### ✅ 5.1. Настройка Edge Certificates

1. В Cloudflare: **SSL/TLS** → **Edge Certificates**
2. Включите:
   - ✅ **Always Use HTTPS**
   - ✅ **Automatic HTTPS Rewrites**

Это обеспечит:
- ✅ Автоматический редирект HTTP → HTTPS
- ✅ Автоматическую замену HTTP ссылок на HTTPS

---

## 🔹 ШАГ 6. (РЕКОМЕНДУЕТСЯ) Ограничить прямой доступ к IP

### ✅ 6.1. Настройка Firewall для Cloudflare

Чтобы сайт открывался только через Cloudflare:

```bash
cd /home/sultangali/Documents/platonus-buketov
sudo ./setup-cloudflare-firewall.sh
```

Скрипт предложит два варианта:
1. **Разрешить доступ только с IP Cloudflare** (рекомендуется, более безопасно)
2. **Разрешить доступ всем на порты 80/443** (проще, но менее безопасно)

**Рекомендуется выбрать вариант 1** для:
- ✅ Повышения безопасности
- ✅ Защиты от прямого доступа по IP
- ✅ Затруднения работы фильтров вуза по IP

### ✅ 6.2. Ручная настройка (если нужно)

```bash
# Разрешаем SSH (ВАЖНО сделать первым!)
sudo ufw allow 22/tcp

# Скачиваем список IP Cloudflare
sudo mkdir -p /etc/cloudflare
sudo curl -s https://www.cloudflare.com/ips-v4 -o /etc/cloudflare/cloudflare-ips-v4.txt

# Разрешаем порты 80 и 443 только для Cloudflare IP
while IFS= read -r ip; do
    sudo ufw allow from "$ip" to any port 80 proto tcp
    sudo ufw allow from "$ip" to any port 443 proto tcp
done < /etc/cloudflare/cloudflare-ips-v4.txt

# Включаем firewall
sudo ufw enable

# Проверяем статус
sudo ufw status verbose
```

---

## 🔹 ШАГ 7. Проверка

### ✅ 7.1. Проверка с разных сетей

Проверьте сайт:

- 📱 **С мобильного интернета**: `https://oxxooy.online`
- 🏠 **С домашней сети**: `https://oxxooy.online`
- 🎓 **Из сети университета**: `https://oxxooy.online`

### ✅ 7.2. Что должно работать

В большинстве вузов:
- ✅ Сайт начинает открываться
- ✅ Фильтр больше не видит "левый VPS"
- ✅ Виден только Cloudflare CDN

### ✅ 7.3. Проверка сертификата

```bash
# Проверка через openssl
openssl s_client -connect oxxooy.online:443 -servername oxxooy.online

# Проверка через curl
curl -I https://oxxooy.online
```

### ✅ 7.4. Проверка в Cloudflare

1. В Cloudflare: **SSL/TLS** → **Overview**
2. Проверьте статус — должен быть зеленый индикатор ✅

---

## 🆘 Если всё ещё блокируют

### Что проверить:

1. **Что именно пишет браузер?**
   - Ошибка подключения?
   - Таймаут?
   - 403 Forbidden?

2. **Открывается ли https://www.cloudflare.com в вузе?**
   - Если нет — проблема с доступом к Cloudflare
   - Если да — проблема в настройках

3. **HTTP код ошибки?**
   - 403 / reset / timeout?

### Возможные решения:

1. **Настройка Cloudflare WAF:**
   - SSL/TLS → **WAF** → настройте правила

2. **Проверка заголовков:**
   - Убедитесь, что Cloudflare передает правильные заголовки

3. **Альтернативный вариант:**
   - Использование другого CDN
   - Настройка через другой прокси

---

## ✅ Итоговая проверка

После всех шагов у вас должно быть:

- ✅ Домен подключен к Cloudflare
- ✅ DNS записи с Proxy (🟠 оранжевое облако)
- ✅ Origin Certificate установлен на сервере
- ✅ Nginx настроен для HTTPS
- ✅ Режим SSL: **Full (strict)**
- ✅ Always Use HTTPS включен
- ✅ Firewall настроен (опционально)

---

## 📚 Полезные команды

```bash
# Проверка DNS
dig oxxooy.online
dig www.oxxooy.online

# Проверка Nginx
sudo nginx -t
sudo systemctl status nginx

# Логи Nginx
sudo tail -f /var/log/nginx/platonus_error.log

# Статус firewall
sudo ufw status

# Проверка сертификата
openssl s_client -connect oxxooy.online:443 -servername oxxooy.online
```

---

## 🎯 Почему это работает

1. **Университет видит Cloudflare, а не Google VPS**
   - IP адрес скрыт за Cloudflare CDN
   - Репутация Cloudflare чистая

2. **TLS-сертификат — глобально доверенный**
   - Cloudflare Origin Certificate доверяется браузерами
   - Режим Full (strict) обеспечивает полное шифрование

3. **Это CDN, а не VPN → формально разрешено**
   - Cloudflare — легитимный CDN сервис
   - Не является VPN или прокси-сервисом

---

**Ты всё делаешь правильно, осталось чуть-чуть дожать 💪**



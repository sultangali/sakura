# Конвертация WMF/EMF в PNG

## Что изменилось

Теперь вместо placeholder "[Formula/Image]" система пытается конвертировать WMF/EMF изображения из DOCX в PNG используя системные инструменты.

## Как это работает

1. **Извлечение изображений** - mammoth извлекает все изображения из DOCX (включая WMF/EMF формулы)
2. **Конвертация WMF/EMF** - система пытается конвертировать в PNG используя:
   - ImageMagick (`convert`)
   - GraphicsMagick (`gm convert`)
   - LibreOffice (`libreoffice --convert-to png`)
3. **Оптимизация** - Sharp обрезает края и уменьшает размер
4. **Сохранение** - PNG файл сохраняется в `/uploads/`

## Установка инструментов конвертации

### Ubuntu/Debian:

```bash
# ImageMagick (рекомендуется)
sudo apt-get update
sudo apt-get install -y imagemagick

# Или GraphicsMagick
sudo apt-get install -y graphicsmagick

# Или LibreOffice
sudo apt-get install -y libreoffice
```

### Проверка установки:

```bash
# Проверка ImageMagick
convert --version

# Проверка GraphicsMagick
gm version

# Проверка LibreOffice
libreoffice --version
```

## Что происходит если инструменты недоступны

Если ни один инструмент не установлен:
- Создается простой белый PNG (без текста placeholder)
- Изображение будет пустым, но хотя бы не будет текста "[Formula/Image]"

## Логирование

При обработке WMF/EMF вы увидите:

```
📷 Обработка изображения: image/x-wmf
🔄 WMF/EMF формат - конвертирую в PNG...
✓ WMF конвертирован в PNG: 1234567890-abc123.png
```

Или если ImageMagick недоступен:

```
⚠ ImageMagick не доступен, пробую GraphicsMagick...
✓ WMF конвертирован через GraphicsMagick: 1234567890-abc123.png
```

## Рекомендации

Для лучшего качества конвертации рекомендуется установить **ImageMagick**:

```bash
sudo apt-get install -y imagemagick
```

Он лучше всего справляется с конвертацией WMF/EMF в PNG.





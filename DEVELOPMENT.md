# Руководство по разработке - Локальный и Облачный режимы

## Автоматическое переключение режимов

Система автоматически определяет режим работы по hostname:

- **Локальный режим** (Development): `localhost` или `127.0.0.1`
- **Облачный режим** (Production): любой другой адрес (например, `34.88.233.59`)

## Локальный режим (Development)

### Запуск для разработки

```bash
# Терминал 1: Запуск сервера
cd server
npm run dev
# Сервер будет доступен на http://localhost:5000

# Терминал 2: Запуск клиента
cd client
npm run dev
# Клиент будет доступен на http://localhost:5173
```

### Что происходит в локальном режиме:

- ✅ Клиент автоматически подключается к `http://localhost:5000`
- ✅ Vite проксирует API запросы на локальный сервер
- ✅ Изображения загружаются с `http://localhost:5000/uploads`
- ✅ Горячая перезагрузка (HMR) включена
- ✅ Детальные ошибки в консоли

### Настройка локального режима

Создайте файл `client/.env.local`:

```env
VITE_SERVER_IP=localhost
VITE_LOCAL_PORT=5000
```

## Облачный режим (Production)

### Развертывание на сервере

```bash
# Запустите скрипт развертывания
sudo ./deploy.sh
```

### Что происходит в облачном режиме:

- ✅ Клиент автоматически подключается к `http://34.88.233.59`
- ✅ Nginx проксирует запросы на Node.js сервер
- ✅ Статические файлы отдаются напрямую из `/client/dist`
- ✅ Изображения загружаются с `http://34.88.233.59/uploads`
- ✅ Оптимизированная сборка для production

### Настройка облачного режима

Создайте файл `client/.env.production`:

```env
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
```

## Ручное переключение режимов

Если нужно принудительно задать режим, отредактируйте `client/src/config.js`:

```javascript
// Принудительно локальный режим
const isDevelopment = true;

// Или принудительно облачный режим
const isDevelopment = false;
```

## Проверка текущего режима

Откройте консоль браузера (F12) - там будет сообщение:
- 🔧 `Режим: ЛОКАЛЬНЫЙ (Development)` - локальный режим
- ☁️ `Режим: ОБЛАЧНЫЙ (Production)` - облачный режим

## Структура конфигурации

```
client/
├── .env.local          # Для локальной разработки (gitignore)
├── .env.production     # Для production сборки (gitignore)
├── .env.example        # Пример конфигурации
└── src/
    └── config.js       # Автоматическое определение режима
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_SERVER_IP` | IP адрес облачного сервера | `34.88.233.59` |
| `VITE_LOCAL_PORT` | Порт локального сервера | `5000` |

## Отладка

### Проверка режима в консоли браузера

```javascript
// В консоли браузера
console.log('API URL:', import.meta.env.VITE_API_URL);
console.log('Server URL:', import.meta.env.VITE_SERVER_URL);
```

### Проверка конфигурации

```bash
# Проверить переменные окружения Vite
cd client
npm run dev -- --mode development  # Локальный режим
npm run build -- --mode production  # Облачный режим
```

## Частые проблемы

### Клиент не подключается к серверу

1. Проверьте, запущен ли сервер: `pm2 status` или `npm run dev` в папке server
2. Проверьте консоль браузера на ошибки CORS
3. Убедитесь, что порты не заняты: `netstat -tuln | grep 5000`

### Неправильный режим определяется

1. Очистите кеш браузера
2. Перезапустите dev сервер: `npm run dev`
3. Проверьте hostname в адресной строке

### API запросы идут не туда

1. Проверьте `client/src/config.js` - там логируется текущий режим
2. Проверьте `.env` файлы в папке `client/`
3. Пересоберите клиент: `npm run build`


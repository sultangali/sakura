# Конфигурация клиента - переключение между серверами

## Простое переключение через один параметр

Теперь для переключения между облачным и локальным сервером нужно изменить только один параметр в `.env.local`:

```env
VITE_SERVER_MODE=cloud  # для облачного сервера
VITE_SERVER_MODE=local  # для локального сервера
```

## Настройка

### Создание `.env.local` файла

В директории `client/` создайте файл `.env.local`:

#### Для облачного сервера:
```env
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_SERVER_MODE=cloud
```

#### Для локального сервера:
```env
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_SERVER_MODE=local
```

## Параметры

| Параметр | Описание | Значения | По умолчанию |
|----------|----------|----------|--------------|
| `VITE_SERVER_MODE` | Режим сервера | `cloud`, `local`, `auto` | `auto` |
| `VITE_SERVER_IP` | IP облачного сервера | IP адрес | `34.88.233.59` |
| `VITE_LOCAL_PORT` | Порт локального сервера | Номер порта | `5000` |

### Режимы работы:

- **`cloud`** - Всегда использовать облачный сервер (`34.88.233.59`)
- **`local`** - Всегда использовать локальный сервер (`localhost:5000`)
- **`auto`** - Автоматическое определение (по hostname)

## Быстрое переключение

### Windows:

**Облачный сервер:**
```batch
start-client-cloud.bat
```

**Локальный сервер:**
```batch
start-client-local.bat
```

### Linux/macOS:

**Облачный сервер:**
```bash
cd client
echo "VITE_SERVER_MODE=cloud" > .env.local
npm run dev
```

**Локальный сервер:**
```bash
cd client
echo "VITE_SERVER_MODE=local" > .env.local
npm run dev
```

## Ручное редактирование

1. Откройте `client/.env.local`
2. Измените строку:
   ```env
   VITE_SERVER_MODE=cloud  # или local
   ```
3. Сохраните файл
4. **Перезапустите Vite** (Ctrl+C и `npm run dev` снова)

## Проверка

После запуска проверьте консоль:

### Для облачного сервера:
```
📋 Vite Configuration:
   SERVER_MODE: cloud
   Use Cloud Server: true
🔄 Vite Proxy Target: http://34.88.233.59
```

### Для локального сервера:
```
📋 Vite Configuration:
   SERVER_MODE: local
   Use Cloud Server: false
🔄 Vite Proxy Target: http://localhost:5000
```

## Примеры файлов

### `.env.local` для облачного сервера:
```env
# Облачный сервер
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_SERVER_MODE=cloud
```

### `.env.local` для локального сервера:
```env
# Локальный сервер
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_SERVER_MODE=local
```

## Важно

- ✅ После изменения `.env.local` **обязательно перезапустите Vite**
- ✅ Файл `.env.local` должен быть в директории `client/`
- ✅ Переменные должны начинаться с `VITE_`
- ✅ Не должно быть пробелов вокруг `=`

## Миграция со старой конфигурации

Если у вас была старая конфигурация с `VITE_FORCE_CLOUD`:

**Старая:**
```env
VITE_FORCE_CLOUD=true
```

**Новая:**
```env
VITE_SERVER_MODE=cloud
```

**Старая:**
```env
VITE_FORCE_LOCAL=true
```

**Новая:**
```env
VITE_SERVER_MODE=local
```


# Исправление проблемы с Vite Proxy

## Проблема

Vite все еще проксирует на `localhost:5000` вместо облачного сервера `34.88.233.59`, даже после создания `.env.local`.

## Причина

Vite загружает переменные окружения только при старте. Если Vite уже запущен, нужно его перезапустить.

## Решение

### Шаг 1: Создайте `.env.local` файл

В директории `client/` создайте файл `.env.local`:

```env
# Локальная разработка с облачным сервером
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_FORCE_CLOUD=true
```

### Шаг 2: Перезапустите Vite

**ВАЖНО:** После создания или изменения `.env.local` файла нужно перезапустить Vite!

1. Остановите Vite (Ctrl+C в терминале)
2. Запустите снова: `npm run dev`

### Шаг 3: Проверьте вывод

После перезапуска в терминале должно показать:

```
═══════════════════════════════════════
📋 Vite Configuration:
   Mode: development
   VITE_SERVER_IP: 34.88.233.59
   VITE_FORCE_CLOUD: true
   Force Cloud: true
🔄 Vite Proxy Target: http://34.88.233.59
═══════════════════════════════════════
```

И при запросах должно показывать:
```
➡️  GET /api/groups -> http://34.88.233.59/api/groups
```

## Автоматическое создание файла

### Windows:

Используйте готовый bat файл:
```batch
start-client-cloud.bat
```

Он автоматически создаст `.env.local` и запустит Vite.

### Linux/macOS:

```bash
cd client
cat > .env.local << EOF
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000
VITE_FORCE_CLOUD=true
EOF

npm run dev
```

## Проверка

1. **Проверьте файл:**
   ```bash
   cat client/.env.local
   ```

2. **Проверьте вывод Vite:**
   - Должно показать `Vite Proxy Target: http://34.88.233.59`
   - Не должно быть `localhost:5000`

3. **Проверьте в браузере:**
   - Откройте консоль (F12)
   - Должно показать `API URL: /api`
   - Запросы должны успешно выполняться

## Если проблема осталась

### Проверьте, что файл в правильном месте

```bash
# Должен быть в client/.env.local
ls -la client/.env.local
```

### Проверьте синтаксис файла

```bash
# Не должно быть пробелов вокруг =
cat client/.env.local
# Правильно:
# VITE_FORCE_CLOUD=true
# Неправильно:
# VITE_FORCE_CLOUD = true
```

### Убедитесь, что перезапустили Vite

Vite не перезагружает `.env` файлы автоматически. Нужен полный перезапуск.

### Проверьте другие .env файлы

Vite загружает файлы в таком порядке (последний имеет приоритет):
1. `.env`
2. `.env.local`
3. `.env.[mode]`
4. `.env.[mode].local`

Убедитесь, что нет конфликтующих файлов:
```bash
ls -la client/.env*
```

## Важно

- ✅ `.env.local` должен быть в директории `client/`
- ✅ После изменения `.env.local` нужно перезапустить Vite
- ✅ Переменные должны начинаться с `VITE_`
- ✅ Не должно быть пробелов вокруг `=`





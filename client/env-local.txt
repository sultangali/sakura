# Режим: ЛОКАЛЬНЫЙ СЕРВЕР (по умолчанию)
# Используйте этот файл когда сервер запущен локально
# Скопируйте в .env.local для активации:
#   cp env-local.txt .env.local

# IP облачного сервера (на случай переключения)
VITE_SERVER_IP=34.88.233.59
VITE_LOCAL_PORT=5000

# Режим работы:
# VITE_FORCE_CLOUD=true  - всегда использовать облачный сервер
# VITE_FORCE_LOCAL=true  - всегда использовать локальный сервер

# Для локальной разработки:
VITE_FORCE_LOCAL=true

